import { KeyboardPositionPipe } from './keyboard-position.pipe';

describe('KeyboardPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyboardPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
