import { KeyboardScrollStrategyPipe } from './keyboard-scroll-strategy.pipe';

describe('KeyboardScrollStrategyPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyboardScrollStrategyPipe();
    expect(pipe).toBeTruthy();
  });
});
