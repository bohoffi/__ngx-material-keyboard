import { NgxMaterialKeyboardDirective } from './ngx-material-keyboard.directive';

describe('NgxMaterialKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMaterialKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
